This directory contains all files pertaining to the FC-1 architecture.  See cpu.txt for a description of the ISA.

'emulator/' contains a C reference emulator.  'assembler/' contains a probably-reusable assembler.  'thll/' contains the Terrible High Level Language, a mildly bad higher-level language that compiles to FC-1 assembly.
